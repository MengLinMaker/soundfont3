import{_ as e,c as a,o as t,a3 as o}from"./chunks/framework.z7kdJ_yr.js";const b=JSON.parse('{"title":"SoundFont2 Structure","description":"","frontmatter":{"prev":{"text":"1.2. Basic usage","link":"/routes/1.%20Guide/1.2.%20Basic%20usage.html"},"next":{"text":"2. API","link":"/routes/2.%20API/README.html"}},"headers":[],"relativePath":"routes/1. Guide/1.3. SF2 structure.md","filePath":"routes/1. Guide/1.3. SF2 structure.md","lastUpdated":1718864318000}'),s={name:"routes/1. Guide/1.3. SF2 structure.md"},n=o('<h1 id="soundfont2-structure" tabindex="-1">SoundFont2 Structure <a class="header-anchor" href="#soundfont2-structure" aria-label="Permalink to &quot;SoundFont2 Structure&quot;">​</a></h1><p>This section explains a bit about the general SoundFont2 structure. It&#39;s not necessarily required to know this information in order to use the library, but it may make it easier to understand how the library and SoundFonts work.</p><h2 id="general-structure" tabindex="-1">General Structure <a class="header-anchor" href="#general-structure" aria-label="Permalink to &quot;General Structure&quot;">​</a></h2><p>A SoundFont has one or more presets, which can normally be selected with the <code>MIDI Program Change</code> command. The presets have one or more preset zones, that have one instrument. The instruments have instrument zones and these have one sample each.</p><p>Multiple preset zones can refer to the same instrument and multiple instrument zones can refer to the same samples.</p><div style="background:WhiteSmoke;padding:1em;border-radius:1em;"><p><img src="https://i.imgur.com/c2Gud3u.png" alt="General Structure"></p></div><h2 id="riff-chunks" tabindex="-1">RIFF Chunks <a class="header-anchor" href="#riff-chunks" aria-label="Permalink to &quot;RIFF Chunks&quot;">​</a></h2><p>A SoundFont2 file is a RIFF (Resource Interchange File Format) file, which contains multiple chunks. Those chunks are identified by a <a href="https://en.wikipedia.org/wiki/FourCC" target="_blank" rel="noreferrer">FourCC (four-character code)</a>. If an identifier is not for characters long, it will be padded by spaces. For example, &quot;foo&quot; would be read as &quot;foo &quot;.</p><p>Every SoundFont2 file should have at least three top-level sub-chunks: an <code>INFO</code> chunk containing metadata, a <code>sdta</code> chunk containing Wave Audio (WAV) samples and a <code>pdta</code> header containing the preset, instrument and sample headers.</p><p>All the raw (unparsed) data is available in the <code>SoundFont2</code> class, as <code>metaData</code>, <code>sampleData</code> and <code>presetData</code>.</p><p><img src="https://i.imgur.com/BL8FvcC.png" alt="SoundFont2 RIFF chunks"></p><h3 id="info-chunk" tabindex="-1"><code>INFO</code> Chunk <a class="header-anchor" href="#info-chunk" aria-label="Permalink to &quot;`INFO` Chunk&quot;">​</a></h3><p>The <code>INFO</code> chunk contains at least the following sub-chunks.</p><ul><li><p><code>ifil</code> - The SoundFont specification version.</p><p>Available as <code>metaData.version</code>.</p></li><li><p><code>isng</code> - The sound engine for which the SoundFont was optimized.</p><p>Available as <code>metaData.soundEngine</code>.</p></li><li><p><code>INAM</code> - The name of the SoundFont.</p><p>Available as <code>metaData.name</code>.</p></li></ul><p>The other sub-chunks are <strong>optional</strong>.</p><ul><li><p><code>irom</code> - A sound data ROM to which any ROM samples refer.</p><p>Available as <code>metaData.rom</code>.</p></li><li><p><code>iver</code> - A sound data ROM revision to which any ROM samples refer.</p><p>Available as <code>metaData.romVersion</code>.</p></li><li><p><code>ICRD</code> - The creation date of the SoundFont, conventionally in the &#39;Month Day, Year&#39; format.</p><p>Available as <code>metaData.creationDate</code>.</p></li><li><p><code>IENG</code> - The author or authors of the SoundFont.</p><p>Available as <code>metaData.author</code>.</p></li><li><p><code>IPDR</code> - The product for which the SoundFont is intended.</p><p>Available as <code>metaData.product</code>.</p></li><li><p><code>ICOP</code> - Copyright assertion string associated with the SoundFont.</p><p>Available as <code>metaData.copyright</code>.</p></li><li><p><code>ICMT</code> - Any comments associated with the SoundFont.</p><p>Available as <code>metaData.comments</code>.</p></li><li><p><code>ISFT</code> - The tool used to create the SoundFont.</p><p>Available as <code>metaData.createdBy</code>.</p></li></ul><h3 id="sdta-chunk" tabindex="-1"><code>sdta</code> Chunk <a class="header-anchor" href="#sdta-chunk" aria-label="Permalink to &quot;`sdta` Chunk&quot;">​</a></h3><p>The sample chunk has one or two sub-chunks.</p><ul><li><p><code>smpl</code> - The 16-bit WAV sample data.</p><p>Available as <code>sampleData</code>.</p></li><li><p><code>sm24</code> - The 8-bit WAV sample data, in addition to the 16-bit data.</p><p>Currently not available in the API.</p></li></ul><p>The <code>sdta</code> and <code>sm24</code> sub-chunks can be combined to get 24-bit WAV sample data, but the library currently does not support this yet.</p><h3 id="pdta-chunk" tabindex="-1"><code>pdta</code> Chunk <a class="header-anchor" href="#pdta-chunk" aria-label="Permalink to &quot;`pdta` Chunk&quot;">​</a></h3><p>The <code>pdta</code> sub-chunk contains data for presets, instruments and samples in the SoundFont. There are four preset and four instrument chunks, which form the instruments and presets. There is just one sample header chunk, that forms the samples together with the data from the <code>sdta</code> chunk.</p><h4 id="preset-data" tabindex="-1">Preset Data <a class="header-anchor" href="#preset-data" aria-label="Permalink to &quot;Preset Data&quot;">​</a></h4><p>The <code>pdta</code> has four sub-chunks that form the preset data.</p><ul><li><p><code>phdr</code> - The preset headers.</p><p>Available as <code>presetData.presetHeaders</code>.</p></li><li><p><code>pbag</code> - The preset zone indices.</p><p>Available as <code>presetData.presetZones</code>.</p></li><li><p><code>pmod</code> - The preset modulators.</p><p>Available as <code>presetData.presetModulators</code></p></li><li><p><code>pgen</code> - The preset generators.</p><p>Available as <code>presetData.presetGenerators</code></p></li></ul><h4 id="instrument-data" tabindex="-1">Instrument Data <a class="header-anchor" href="#instrument-data" aria-label="Permalink to &quot;Instrument Data&quot;">​</a></h4><p>The <code>pdta</code> has four sub-chunks that form the instrument data.</p><ul><li><p><code>inst</code> - The instrument headers.</p><p>Available as <code>presetData.instrumentHeaders</code>.</p></li><li><p><code>ibag</code> - The instrument zone indices.</p><p>Available as <code>presetData.instrumentZones</code>.</p></li><li><p><code>imod</code> - The instrument modulators.</p><p>Available as <code>presetData.instrumentModulators</code></p></li><li><p><code>igen</code> - The instrument generators.</p><p>Available as <code>presetData.instrumentGenerators</code></p></li></ul><h4 id="sample-data" tabindex="-1">Sample Data <a class="header-anchor" href="#sample-data" aria-label="Permalink to &quot;Sample Data&quot;">​</a></h4><p>The <code>pdta</code> has one sub-chunk with the sample header data.</p><ul><li><p><code>shdr</code> - The sample headers.</p><p>Available as <code>presetData.sampleHeaders</code></p></li></ul><h2 id="presets" tabindex="-1">Presets <a class="header-anchor" href="#presets" aria-label="Permalink to &quot;Presets&quot;">​</a></h2><p>TODO</p><h2 id="instruments" tabindex="-1">Instruments <a class="header-anchor" href="#instruments" aria-label="Permalink to &quot;Instruments&quot;">​</a></h2><p>TODO</p>',35),r=[n];function d(i,c,l,p,h,u){return t(),a("div",null,r)}const f=e(s,[["render",d]]);export{b as __pageData,f as default};
